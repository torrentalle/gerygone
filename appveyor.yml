init:
  - set PATH=%PATH%;C:\MinGW\bin;%GOPATH%\bin
  - go version
  - go env

# clones and cd's to path
clone_folder: %GOPATH%\src\github.com\torrentalle\gerygone

install:
  - set PATH=%PATH%;%GOPATH%\bin
  - go get -v github.com/magefile/mage
  - dir /s %GOPATH%

build_script:
  - mage vendor gerygoneRace
  - mage -v check
  - gerygone
